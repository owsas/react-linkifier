var webpack = require('webpack');
var defaultConfig = require('./webpack.config.js');

module.exports = Object.assign(defaultConfig, {
    output: {
        path: __dirname + '/dist',
        filename: 'linkifier.min.js',
        library: 'linkifier',
        libraryTarget: 'umd'
    },
    plugins: [
        new webpack.optimize.UglifyJsPlugin({
            compress: {
                warnings: true
            },
        }),
    ]
});
